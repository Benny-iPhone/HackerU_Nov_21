25.1.2022 homework:

1) create an app with a view that rotates forever
(modify the view transforms by a small rotation every small interval using timer)

2) Enrich DrawSomethingApp project with
a. save the draw as an image to library (UIView to UIImage, save UIImage to library)
b. select pencil size
c. select color
d. undo (array of bezierPath instead of 1)
e. redo